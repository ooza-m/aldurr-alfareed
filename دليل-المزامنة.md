# تفعيل المزامنة السحابية — الخطوات المتبقية

إعدادات Firebase رُكّبت في `index.html` بالفعل ✅
يتبقّى ثلاث خطوات في لوحة Firebase (مرة واحدة).

---

## ١) تفعيل تسجيل الدخول بالبريد

1. افتحي https://console.firebase.google.com واختاري مشروع **durr-alfareed**.
2. من القائمة الجانبية: **Build ← Authentication ← Get started**.
3. اختاري **Email/Password**، فعّلي المفتاح الأول ← **Save**.

## ٢) إنشاء قاعدة البيانات + قواعد الأمان

1. **Build ← Firestore Database ← Create database**.
2. اختاري **Start in production mode ← Next**، ثم أقرب موقع ← **Enable**.
3. افتحي تبويب **Rules**، امسحي المحتوى وألصقي هذا:

```
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{userId}/{document=**} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
```

4. **Publish**.

> معنى القاعدة: كل شخص يرى ويعدّل بياناته هو فقط.

## ٣) السماح لنطاق موقعك

1. **Authentication ← Settings ← Authorized domains ← Add domain**.
2. أضيفي نطاق موقعك على GitHub: `اسم-حسابك.github.io`
3. **Add**.

---

## الاستخدام

- في اللوحة، اضغطي **☁️ المزامنة السحابية**.
- أول مرة: **حساب جديد**، بريدك وكلمة سرّ (٦ أحرف على الأقل) ← دخول.
- على أي جهاز آخر: افتحي نفس الرابط ← المزامنة ← **دخول** بنفس البيانات.
- من الآن، كل تعديل يُحفظ في السحابة تلقائيًا ويظهر على أجهزتك.

### ملاحظات
- إن كان جهازان مفتوحين، اضغطي **⬇ جلب من السحابة** في الثاني لجلب آخر نسخة.
- تبقى نسخة محلية على كل جهاز لو انقطع الإنترنت.
- المفاتيح في الملف آمنة للنشر — الحماية من قواعد الأمان أعلاه.

> عند أي عقبة: أرسلي لي لقطة شاشة وأرشدك.
